# 测试范围

## 项目信息

- 项目名称：devnu11-cn-e2e
- 负责人：user
- 创建时间：2026-06-02
- 状态：已确认授权范围；泛解析范围应执行主动 DNS 子域名枚举，等待确认 DNS 执行参数

## 授权信息

- 授权来源：用户在当前 Codex 线程中声明
- 授权人：域名和服务器所有者
- 授权窗口：待 Claude Code 执行主动 DNS 或 HTTP 探测前按场景再次确认
- 紧急联系人：当前用户

## 主动验证前置条件

| 条目 | 当前状态 | 备注 |
| --- | --- | --- |
| 授权域名 | 已确认 | `*.devnu11.cn`，用户声明为自有域名和服务器；`*` 代表泛解析/通配范围，应执行主动 DNS 子域名枚举 |
| 主动 DNS 子域名枚举 | 应执行，待确认执行参数 | 不需要实际访问 URL；执行前需确认授权窗口、DNS 并发/速率、resolver 来源和停止条件 |
| 实际访问 URL | HTTP/端口/API 待提供 | 仅阻塞 HTTP 存活、端口、路径/API 和漏洞验证；不阻塞主动 DNS 子域名枚举 |
| 授权窗口 | 待确认 | DNS 枚举窗口和 HTTP/端口/API 探测窗口可以分开确认 |
| 允许请求速率 | 待确认 | DNS 并发/速率与 HTTP 请求速率分开记录；未确认 DNS 速率前不执行主动 DNS |
| 测试账号 | 待提供 | 认证后 Web/API 测试需要用户提供测试账号或确认允许注册 |
| 管理员账号 | 待提供 | 管理后台 API 权限验证需要单独授权，不默认拥有 |

## 测试范围

| 类型 | 目标 | 备注 |
| --- | --- | --- |
| web/api | `*.devnu11.cn` | 用户声明这是自己的域名和服务器，允许进行授权测试 |

## 不在范围内

| 目标或动作 | 原因 |
| --- | --- |
| 非 `devnu11.cn` 及其子域名的第三方资产 | 未授权 |
| DoS / DDoS / 压测 / 大流量扫描 | 高风险，可能影响服务可用性 |
| 破坏性写入、删除数据、持久化后门 | 超出验证目的 |
| 社会工程学、钓鱼、凭据填充 | 用户未授权 |
| 云厂商元数据、第三方 SaaS、供应商系统 | 除非后续用户明确授权，否则不测试 |

## 允许的操作

- 被动信息收集
- 受控主动 DNS 子域名字典枚举（确认 DNS 并发/速率和 resolver 后）
- 低频 HTTP / 端口 / 路径 / API 信息收集（确认实际 URL、窗口和 HTTP 速率后）
- 手工 Web / API 测试
- 已提供凭据的低频认证测试
- 使用项目正式 skill 指导测试和记录

## 禁止的操作

- 拒绝服务测试
- 社会工程学攻击
- 钓鱼
- 凭据填充
- 持久化或破坏性操作
- 测试范围外目标
- 未确认 DNS 并发/速率前进行主动 DNS 子域名枚举
- 未确认实际 URL、HTTP 速率和授权窗口前进行 HTTP/端口/路径/API 主动探测

## 测试账号

| 角色 | 用户名 | 备注 |
| --- | --- | --- |
| 待提供 | 待提供 | 如需认证测试，由用户另行提供 |

## 速率限制与安全

- DNS 并发/速率：待用户确认；确认后可对 `devnu11.cn` 授权子域范围执行主动 DNS 子域名枚举
- HTTP 请求速率：待用户确认；未确认实际 URL 和 HTTP 速率前不执行 HTTP 存活、端口、路径/API 或服务指纹探测
- 高风险操作需确认：是
- 数据处理说明：只记录最小必要证据；不得保存真实敏感数据、凭据或个人信息

## 确认

- 测试者已审阅范围：否
- 已与用户确认范围：是
- 备注：Codex 仅准备 scope 和交接材料，不对目标发起探测；端到端验证由 Claude Code 执行。主动 DNS 子域名枚举应在确认 DNS 授权窗口、DNS 并发/速率和 resolver 后执行；实际 URL、HTTP 速率和账号状态仅阻塞 HTTP/端口/路径/API 探测与漏洞验证。
