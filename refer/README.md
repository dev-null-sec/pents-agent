# refer

本目录用于存放本机参考资料和第三方原始归档，例如 Xalgorix、fuzzDicts 等。

这些资料体量较大、文件数量多，不直接进入本项目 Git 仓库。需要长期维护的正式资产应从参考资料中精选或同步到项目目录：

- `dicts/`：项目维护的字典层和候选回灌区
- `skills/`：正式 Claude Code skill
- `tools/`：自写脚本和工具说明
- `docs/`：路线、决策和项目状态

如果后续确实需要固定某个第三方参考版本，优先记录来源、commit/tag、同步时间和用途，不要默认把整个原始仓库提交进来。
