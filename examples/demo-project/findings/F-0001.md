# 漏洞记录：示例调试接口暴露候选

## 元数据

- 编号：F-0001
- 项目：demo-project
- 状态：candidate
- 严重程度：Low
- CVSS 评分：待评估
- 影响目标：`https://api.example.test/api/v1/debug/status`
- 发现时间：2026-06-03 18:40:00
- 确认人：未确认

## 概述

JS 静态分析示例中出现 `/api/v1/debug/status` 路由。由于当前 demo 不执行真实 HTTP 请求，该 finding 仅作为候选记录，用于展示证据不足时的写法。

## 受影响组件

| 组件 | URL 或端点 | 参数 | 备注 |
| --- | --- | --- | --- |
| Debug API | `/api/v1/debug/status` | 无 | 示例端点，未实际验证 |

## 前置条件

- 授权范围包含 `api.example.test`。
- 测试账号或访问级别：未使用。
- 所需用户交互：无。

## 复现步骤

1. 在 JS 静态分析摘要中发现 `/api/v1/debug/status`。
2. 确认该端点属于 `api.example.test` 示例 API 范围。
3. 因未执行真实 HTTP 请求，将状态保持为 candidate。

## 证据

| 证据编号 | 类型 | 路径或引用 | 备注 |
| --- | --- | --- | --- |
| E-0003 | js-summary | runs/R001-2026-06-03-demo-recon/outputs/static-js-summary.md | 静态线索 |
| E-0004 | request-summary | requests/debug-status-redacted.txt | 示例脱敏引用，证据链不完整 |

## 影响

如果真实环境中该端点无需认证且返回调试信息，可能造成版本、环境变量或内部状态暴露。本 demo 未确认该行为。

## 修复建议

确认真实环境中调试端点是否存在；若存在，应要求认证、限制环境、移除敏感字段，并在生产环境禁用调试接口。

## 验证状态

- 复测状态：未复测
- 复测备注：等待 HTTP 低频授权后再确认。

## 安全声明

- 未测试范围外目标。
- 未执行破坏性操作。
