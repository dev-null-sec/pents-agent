# 当前状态

> 给下一次 AI 或人类开发者快速接上上下文用。

## 项目一句话

基于 Claude Code 的轻量 Web/API 授权测试工作台，用少量高质量 skill、Markdown 模板和 `pents` CLI 替代传统 Web UI 渗透平台。

## 当前能力

- ai-board 项目管理已初始化
- 项目根目录已初始化 Git 仓库；`refer/` 原始参考资料作为本机资料不纳入版本库，正式资产沉淀到 `dicts/`、`skills/`、`tools/` 和 `docs/`
- Xalgorix 源码已归档到 `refer/xalgorix/`
- fuzzDicts 已作为原始字典参考库归档到 `refer/fuzzDicts/`
- CLAUDE.md 顶层规则已编写
- 项目方向已从“全量粗筛 300+ skill”收紧为“Web/API MVP”
- `skills/` 已清空批量迁移旧 skill，并将中文正式 skill 扩展到 23 个；recon 相关 skill 已吸收 devnu11 实战反馈
- Xalgorix 原始 skill 保留在 `refer/xalgorix/internal/tools/skills/data/`
- 已补充 `pents` 提效定位和 Claude Code 子代理协作路线
- `templates/` 已有第一版项目模板；progress/review/report/brief 已补充 skipped/blocker 结构化反馈字段
- `templates/` 已补充 run 级模板：`run-progress.md`、`run-report-delta.md`、`run-review.md`
- 已建立资产图谱与入口卡片路线：`docs/项目路线/资产图谱与入口卡片.md` 定义 domain/subdomain/ip/url/service/app/fingerprint/api/evidence/risk 等实体关系；`templates/asset-card.md` 提供可测试入口卡片模板，`inventory.md` 增加入口卡片索引
- 已建立 browser-test-agent 流程、正式 skill 与任务卡模板：`skills/recon/browser-test-agent/SKILL.md` 和 `docs/项目路线/browser-test-agent流程.md` 定义 Claude Code 调用 agent-browser 的登录/注册/表单/导航/截图/网络摘要/证据回填流程；主 agent 默认基于 snapshot/DOM/network 判断，遇到验证码、Turnstile、canvas、图片化页面、遮挡或截图脱敏时优先调用 `pents vision-review`，不再默认依赖 Claude Code 内置视觉子代理；流程已通过 hiwonder R001 做过一次公开页面 canary
- `projects/hiwonder-cn-canary/` 已完成 R001/R002 视觉 canary：R001 证明 agent-browser open/screenshot/snapshot --full 正常，页面为“幻尔后台管理”登录页；R002 证明 Claude Code 内置 haiku/mimo 图片 Read 链路会在 ai.devnu11.cn 网关持续 502/超时，不能作为默认视觉链路
- 已补齐 T-0058 的 Claude Code 明确任务卡：`projects/hiwonder-cn-canary/briefs/claude-t0058-mimo-vision-canary.md`。R002 首次新会话执行暴露出“只有看板条目、没有可读任务卡”和“agent-browser 不可用时会自行改用 WebFetch/系统浏览器/Playwright”等问题；现已在任务卡、`skills/recon/browser-test-agent/SKILL.md` 和 `templates/browser-test-agent-brief.md` 中明确：`agent-browser` 是唯一浏览器链路，不可用就记录 `tool_missing_or_unavailable` 并停止，不得安装或使用替代截图工具。`projects/hiwonder-cn-canary/runs/R002-2026-06-04-mimo-vision-canary/` 已保存 `pents vision-review` smoke JSON，可继续作为正式 R002 回填目录使用
- `pents vision-review` 已成为默认视觉识别适配层：输入本地截图和窄问题，调用 OpenAI-compatible vision API，输出固定 JSON；API key 只从本地 `.env` / `.env.local` 或环境变量读取，不写入任务卡、日志或报告；`.env.example` 已提供模板，真实 `.env` 已加入 `.gitignore`；支持 `--mock` 本地验证、超时和错误 JSON，避免 Claude Code 内置视觉子代理读图卡死拖死主流程。实测 token-plan-cn 网关下 `mimo-v2.5` 可读图，14.877 秒识别出后台登录页、验证码图片、用户名/密码/验证码输入框、保持会话和登录按钮；`mimo-v2.5-pro` 对图片入口返回 404 `No endpoints found that support image input`
- `dicts/` 已建立默认字典层：子域名主字典原样同步 fuzzDicts `subdomainDicts/main.txt`，目录/API 路径和参数名保留小字典，以及 candidates 回灌区
- `tools/` 已建立资产结构：自写脚本按 recon/evidence/dicts/common 分类；第三方工具按 install/vendor/patches/bin/cache/dist 分层管理，源码/fork/补丁可入仓，运行二进制默认本地化管理
- `pentest-intel-hub/` 已建立为独立情报与知识蒸馏模块：服务于主项目，但不直接执行攻击、不直接改正式 skill；当前只包含结构、策略、知识卡片模板、验证/晋升规则和 exports 集成协议，尚未实现爬虫或联网采集
- `tools/recon/active-dns-massdns.ps1` 已成为主动 DNS 默认执行脚本，使用候选文件驱动 massdns，不走 puredns stdin pipe，并记录各阶段耗时、退出码、候选数和命中数
- `skills/recon/subdomain-enumeration` 已同步主动 DNS 新口径：默认 `massdns direct`；`puredns`、`shuffledns`、`dnsx` 只作为可选 wrapper、替代或诊断；`massdns -o Snl` 不输出 NOERROR/NODATA，需用小列表 `dnsx` 复核时单独记录
- `cli/` 已实现 `pents` 最小 CLI 脚手架，可通过 `uv run --project cli pents` 运行；已增加 `pents doctor-recon`、`pents active-dns`、`pents run new`、`pents static-js`、`pents vision-review`、`pents evidence` 证据链字段、`pents report` 汇总能力、`pents merge`、`pents review-agent-output` 和 `pents suggest-skills`；`pents active-dns` 已支持显式 `--record-types A,AAAA,CNAME` 和可选 `--nodata-check`，默认仍只走精简 `A` 记录 massdns direct
- 职责边界已明确：ai-board 只由 Codex / 项目开发者维护开发计划；Claude Code 只是渗透测试执行主体，不操作 ai-board，只读取任务卡、scope、run 文档并回填项目记录
- `projects/devnu11-cn-e2e/` 已完成第一轮静态/被动端到端验证和第二轮低频 recon 被动部分；R001 由 Codex 在前置链路验证中误执行了完整主动 DNS 枚举，结果只作为工具链路可用性和 Claude Code 正式执行的对照基线，不作为“Claude Code 已正式执行”的验收；T-0022 低频 HTTP/CDN、服务指纹和小范围 Web 端口验证已完成规划更新与任务卡编写，尚未执行

## 当前技术栈

- Claude Code（执行引擎）
- Python 3.10+（pents CLI）
- Markdown（所有文档/模板/记录）
- ai-board（项目管理，仅 Codex / 项目开发者使用）

## 重要目录

- `skills/` — 正式 Claude Code skill 武器库（recon 4 + web 14 + api 5）
- `templates/` — scope/progress/inventory/evidence/finding/report/review/brief 模板
- `projects/` — 渗透项目存档
- `refer/xalgorix/` — Xalgorix 参考资料
- `refer/fuzzDicts/` — fuzzDicts 原始字典参考库，不默认全量用于扫描
- `dicts/` — 项目精选字典和复盘回灌候选
- `cli/` — pents Python CLI 包
- `pentest-intel-hub/` — 独立安全情报摄取、评分、知识卡片、验证和候选输出模块
- `docs/` — ai-board 项目文档

## 最近完成

- [x] 项目方向初次确认（Python CLI、pents、Claude Code 执行底座）
- [x] CLAUDE.md 编写
- [x] 项目方向.md 编写
- [x] 框架目录结构创建
- [x] v1 方向收紧：先做 Web/API MVP，不追求全量 300+ skill
- [x] 清理批量迁移旧 skill，保留 `refer/xalgorix/` 原始资料
- [x] 补充 `pents` 提效层和子代理协作路线
- [x] 模板与数据结构第一版
- [x] pents CLI 最小命令实现
- [x] 核心 skill 质量治理：`docs/项目路线/skill质量标准.md` 已制定，中文为默认语言
- [x] 模板全部汉化：8 个模板文件均为简体中文
- [x] CLI 中文适配：全角冒号兼容、UTF-8 编码修复、3 个测试全部通过
- [x] 首批 22 个正式 skill 已入库：recon 3 + web 14 + api 5，均按质量标准中文重写
- [x] 已记录 `*.devnu11.cn` 授权范围
- [x] Claude Code 已完成第一轮静态/被动端到端验证：JS 静态分析、被动 crt.sh 查询、3 个 recon skill 调用、F-0001 候选 finding、report/review
- [x] 已补充主动验证前置条件记录：实际访问 URL、授权窗口、允许请求速率、测试账号、管理员账号均需在主动探测前确认
- [x] 已补充低频 recon 交接材料：子域名、端口、CDN 判断、源站线索和服务指纹检查清单已写入 devnu11 项目文档
- [x] Claude Code 已执行低频 recon 被动部分：5 个被动来源均无结果，识别 Sub2API，发现 Cloudflare Turnstile 线索
- [x] 已建立 fuzzDicts 参考库和字典闭环：`refer/fuzzDicts/`、`dicts/curated/`、`dicts/candidates/`
- [x] 已按最新策略同步子域名字典：`dicts/curated/subdomains-main.txt` 原样来自 `refer/fuzzDicts/subdomainDicts/main.txt`，后续再做分层优化
- [x] 已修正主动 recon 速率口径：主动 DNS 子域名枚举走受控批量解析；HTTP、路径/API、端口和源站验证才按低频或专项授权执行
- [x] 已新增 `pents doctor-recon`：可检查 subfinder/dnsx/httpx/shuffledns/subzy/massdns 缺失，输出影响、Windows/WSL/Linux/macOS 安装建议和本机代理提醒
- [x] 已规范 recon 跳过/阻塞反馈：progress/review/report/brief 模板和 devnu11 项目记录都已包含原因类型、已尝试来源、未执行项、影响、建议安装工具和需要用户补充的信息
- [x] 已完成 recon skill 实战反馈修订：subdomain-enumeration 增加被动无结果诊断和 Cloudflare/通配符场景；javascript-analysis 增加已下载 JS 直接分析、软件识别、配置变量、CDN/WAF 线索；api-discovery 增加 JS bundle 静态 API 推断和前后置关系
- [x] 已沉淀 Sub2API 类开源应用指纹与验证路线：从 JS、路由、默认配置识别软件，并规划 setup、admin API、OAuth、支付、默认配置的授权验证路径
- [x] 已建立复测 runs 分层路线：顶层文档记录累计事实，`projects/<name>/runs/Rxxx-<date-purpose>/` 记录单轮进度、证据、编号报告增量和复盘
- [x] 已建立 tools 自写脚本结构和职责边界：CLI 负责编排记录，tools 放可复用脚本，第三方工具只检查安装
- [x] 已更新模板和报告同步规则：progress/report 使用固定状态，report 增加同步说明，CLAUDE.md 要求每轮执行后同步或记录未同步原因
- [x] 已实现 `pents run new`：可自动创建 `runs/R001-<date-purpose>/`，生成 run 级 brief/progress/evidence/编号 report-delta/review 和 outputs/raw
- [x] 已实现 `pents static-js`：可分析本地已下载 JS，提取路由、API、参数、OAuth、支付、配置/CDN/WAF 线索，并可保存到 run outputs
- [x] 已增强 `pents evidence`：支持记录来源 URL、响应头摘要、本地证据文件 SHA256、采集时间和证据类型；缺少来源或 hash 时会提示证据链不完整
- [x] 已升级 `pents report`：可汇总多个 finding 的严重程度统计、candidate/confirmed 状态、证据引用，并提示缺少证据、缺少复现步骤或证据链不完整
- [x] 已实现 `pents merge` 与 `pents review-agent-output`：可读取 JSON 或 fenced JSON 子代理输出，合并 tested_targets / blocked_or_skipped / potential_findings / confirmed_findings 到 inventory/progress，并审查越界风险、证据不足、重复项和候选 finding
- [x] 已实现 `pents suggest-skills`：可根据关键词或测试面从 `skills/README.md` 推荐正式 skill，输出 skill 路径、适用场景摘要和建议 agent role，并覆盖 Web/API/Recon 推荐测试
- [x] 已修正 `*.devnu11.cn` 泛解析 recon 口径：主动 DNS 子域名枚举不再等待实际访问 URL；只需先确认 DNS 授权窗口、DNS 并发/速率、resolver 和停止条件，HTTP/端口/API 探测再等待实际 URL 与 HTTP 速率
- [x] 已建立 Git 项目管理基线：新增 `.gitignore` 和 `refer/README.md`，排除缓存、虚拟环境、原始参考库和大体积原始输出
- [x] 已完成 R001 Codex 工具链路前置验证：实际误跑完整 `subdomains-main.txt` 167377 词条，约 99 秒完成，命中 `ai.devnu11.cn`、`blog.devnu11.cn`、`lk.devnu11.cn`、`online.devnu11.cn`、`st.devnu11.cn`；该结果仅作前置验证和对照基线
- [x] 已复盘主动 DNS 扫描链路问题：dnsx `-wd` 误用会导致未真正执行字典枚举，后续应将 canary、泛解析检测和字典枚举拆开
- [x] 已将主动 DNS 默认链路改为 `massdns direct`：`pents active-dns --engine auto` 默认生成 PowerShell 脚本计划，`puredns`、`dnsx`、`shuffledns` 仅作为可选诊断或替代；该改造用于规避 Claude Code 场景下 `puredns -> massdns` stdin pipe 卡死
- [x] 已修正 Codex / Claude Code 职责边界：Claude Code 任务卡不再要求 ai-board 申领或归档；ai-board 由 Codex 验收维护，CC 只执行渗透流程和回填项目记录
- [x] T-0042 已完成：Claude Code 于 2026-06-04 执行 massdns direct 主动 DNS 复测，60.669 秒，4 命中与 R001 基线一致；结果已回填 run 文档和顶层 progress/inventory/evidence/report/review
- [x] 已完成 T-0022 执行前规划更新：用户确认所有已发现子域名可测试，速率由执行主体按安全区间自定并加入随机抖动；已新增任务卡 `projects/devnu11-cn-e2e/briefs/claude-t0022-low-frequency-validation.md`；本轮未执行探测
- [x] 已建立 `pentest-intel-hub/` 独立情报模块骨架：定位为情报摄取、清洗、评分、知识卡片、验证晋升和候选输出；当前只做结构和规则，不写爬虫、不联网采集、不直接修改主项目正式 skill
- [x] 已完成 browser-test-agent 流程设计：新增浏览器交互测试路线文档和任务卡模板，明确 Claude Code 使用 agent-browser 执行，pents/项目文档负责记录截图、网络摘要、入口卡片和 finding 候选；本轮未打开浏览器、未访问真实目标
- [x] 已完成 browser-test-agent 自动视觉 fallback 机制设计：新增正式 skill 和 `pents vision-review` CLI，明确主 agent 应自动判断何时需要图片识别；视觉主链路改为截图文件 -> CLI/API -> JSON，内置 `vision-reviewer` 子代理只作为明确 canary/对比链路
- [x] T-0056 / hiwonder R001 视觉 canary 已由 Claude Code 执行完成：边界控制正常，未登录、未提交、未绕验证码、未做漏洞测试；当前模型配置下视觉 fallback 不可用，失败分支和 evidence/review 回填有效
- [x] T-0058 / hiwonder R002 mimo 视觉 canary 已由 Claude Code 执行完成：haiku (mimo-v2.5-pro) 纯文本可达且正常（~5.6s），但 Claude Code 内置图片 Read 请求在 ai.devnu11.cn 网关持续 Cloudflare 502（连续 3 次，每次 ~600s 超时）；补充直连 API 测试确认 token-plan-cn 下 `mimo-v2.5` 可读图并返回结构化识别结果，`mimo-v2.5-pro` 在该网关下不是图片入口

## 当前风险 / 未决问题

- 第一轮验证未获得测试账号，认证后 Web/API、管理员权限、OAuth 和支付流程仍为待确认
- 信息收集执行缺口：被动 recon 和 Claude Code 正式主动 DNS 已完成；HTTP/CDN、小范围 Web 端口、服务指纹和 F-0001 极小范围验证已完成规划授权，等待 Claude Code 按任务卡执行
- 子域名枚举流程已修正为分场景控速；工具缺失检查和 recon 跳过/阻塞反馈模板已补齐
- `pents` 仍是最小可用版本，已新增 `vision-review` 视觉 API 适配命令；尚未实现更复杂的结构化解析和 run 结果合并
- `pents` 尚未实现 `report --delta` 或 run 结果合并；`run new` 已可创建编号 run 目录
- `tools/` 已开始沉淀可复用 recon 脚本；后续仍需 evidence hash、subdomain-stats 和结果分类等脚本
- R001 发现 `snapshot -i` 为空不代表页面无内容；`browser-test-agent` skill 和任务卡模板已补充先尝试 `snapshot --full` 的回退逻辑，路线文档同步仍保留在 T-0059
- R002 发现 `ai.devnu11.cn` 网关对 Claude Code 内置 haiku/mimo 图片 Read 请求持续返回 Cloudflare 502（ray_id 已记录），但直连 `token-plan-cn.xiaomimimo.com/v1` 的 `mimo-v2.5` 图片识别正常；后续默认绕开内置视觉子代理，走 `pents vision-review`
- 当前 Git 仅为本地仓库，尚未配置远程地址；如需多人协作或跨机器同步，后续需要确认远程仓库和敏感资料边界

## 下次接手建议

先读 `docs/计划看板.md`，按“正在进行/下一批”推进。建议下一步优先做：

1. 将 T-0058 交给 Claude Code 收尾或复跑前，先在项目根目录按 `.env.example` 创建本地 `.env`；然后让 CC 使用 `pents vision-review` 做图片识别，不要再让内置 haiku 子代理直接 Read 图片。
2. 处理 T-0059：把 browser-test-agent 的 `snapshot -i` 空结果回退到 `snapshot --full` 的逻辑同步到路线文档，并复核 skill、任务卡模板和路线文档三者一致。
3. 继续 devnu11 主线时，再将 `projects/devnu11-cn-e2e/briefs/claude-t0022-low-frequency-validation.md` 交给 Claude Code 执行；短提示为”读取该任务卡，按任务卡执行；不要操作 ai-board”。
